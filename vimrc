set nocompatible

syntax on

set autoindent     " tab in as far as the line above
set expandtab      " turn tabs into spaces
set tabstop=2      " 2 spaces per tab

set path+=**
set runtimepath+=~/.vim/bundle/nerdtree

